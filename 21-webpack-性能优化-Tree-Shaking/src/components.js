const createTitleCpn = () => {
  const element = document.createElement('h2')
  element.className = 'title'
  element.innerHTML = '通过 JavaScript 生成的标题'
  return element
}

// element.className = 'chen'

export default createTitleCpn
