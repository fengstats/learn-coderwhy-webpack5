// 预设
const presets = [
  [
    '@babel/preset-env',
    {
      // polyfill
      // useBuiltIns: 'usage',
      // corejs: 3,
    },
  ],
]

// 插件
const plugins = []

module.exports = {
  presets,
  plugins,
}
